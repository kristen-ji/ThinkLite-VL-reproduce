data:
  train_files: hiyouga/math12k@train
  val_files: hiyouga/math12k@test
  prompt_key: problem
  answer_key: answer
  image_key: images
  video_key: videos
  image_dir: null
  video_fps: 2.0
  max_prompt_length: 224  # Increased slightly to include more samples
  max_response_length: 112 # Increased slightly to include more samples
  rollout_batch_size: 2  # Conservative batch size for 7B model
  mini_rollout_batch_size: 2  # Conservative batch size for 7B model
  val_batch_size: 2  # Conservative batch size for 7B model
  format_prompt: ./examples/format_prompt/math.jinja
  override_chat_template: null
  shuffle: true
  seed: 1
  min_pixels: 65536   # Reduced to 64K pixels to include more images
  max_pixels: 2097152 # Increased to 2M pixels to include more images
  filter_overlong_prompts: false  # Disabled to prevent over-filtering

algorithm:
  adv_estimator: grpo  # Use GRPO algorithm as intended
  disable_kl: false
  use_kl_loss: true
  kl_penalty: low_var_kl
  kl_coef: 1.0e-2
  kl_target: 0.02          # ~0.02 nats/token target
  kl_type: adaptive
  online_filtering: false  # dapo filter groups
  filter_key: overall
  filter_low: 0.01
  filter_high: 0.99

worker:
  actor:
    global_batch_size: 2  # Further reduced for 7B memory constraints
    micro_batch_size_per_device_for_update: 1  # FURTHER REDUCED from 2
    micro_batch_size_per_device_for_experience: 1  # ULTRA-CONSERVATIVE for 7B model memory
    max_grad_norm: 1.0
    padding_free: true
    dynamic_batching: true
    ulysses_size: 1
    model:
      model_path: Qwen/Qwen2.5-VL-7B-Instruct
      enable_gradient_checkpointing: true
      trust_remote_code: false
      freeze_vision_tower: true  # ENABLED to save memory
    optim:
      lr: 1.0e-6
      weight_decay: 1.0e-2
      strategy: adamw_bf16
      lr_warmup_ratio: 0.0
    fsdp:
      enable_full_shard: true
      enable_cpu_offload: true  # Enable FSDP CPU offloading for memory efficiency
      enable_rank0_init: true
      torch_dtype: bf16
    offload:
      offload_params: false  # Disable generic offloading - use FSDP offloading instead
      offload_optimizer: false  # Disable generic offloading - use FSDP offloading instead

  rollout:
    n: 2  # Required for GRPO algorithm
    temperature: 0.7 #Lower rollout randomness
    top_p: 1.0
    limit_images: 0
    gpu_memory_utilization: 0.5  # Increased to 50% to provide sufficient memory for VLLM KV cache
    enforce_eager: true  # ENABLED to bypass CUDA graphs and avoid capture errors
    enable_chunked_prefill: true  # ENABLED for better memory efficiency
    tensor_parallel_size: 1  # Use single GPU per model for memory efficiency
    disable_tqdm: false
    max_num_batched_tokens: 448  # Sufficient for max_prompt_length (224) + max_response_length (112) = 336
    val_override_config:
      temperature: 0.6
      top_p: 0.95
      n: 1

  ref:
    fsdp:
      enable_full_shard: true
      enable_cpu_offload: true  # Enable FSDP CPU offloading for memory efficiency
      enable_rank0_init: true
    offload:
      offload_params: false  # Use FSDP offloading instead

  reward:
    reward_type: batch  # Use batch reward with simplified math function
    reward_function: ./examples/reward_function/math.py:compute_score
    num_cpus: 4  # Increased significantly to avoid resource starvation

trainer:
  total_epochs: 15
  max_steps: null
  project_name: easy_r1
  experiment_name: qwen2_5_grpo_phase3
  logger: ["console", "wandb"]
  nnodes: 2  # PHASE 3: Two-node setup
  n_gpus_per_node: 4
  max_try_make_batch: 10  # REDUCED from 20
  val_freq: 5  # -1 to disable
  val_before_train: true
  val_only: false
  val_generations_to_log: 3
  save_freq: 5  # -1 to disable
  save_limit: 3  # -1 to disable
  save_model_only: false
  save_checkpoint_path: null
  load_checkpoint_path: null
  find_last_checkpoint: true
